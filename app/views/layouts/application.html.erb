<!DOCTYPE html>
<html>
<head>
  <title>SortingHat</title>
  <%= stylesheet_link_tag "https://fonts.googleapis.com/css?family=Seymour+One" %>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1"></head>
<body>
  <div class = "container">
    <% flash.each do |name, msg| %>
      <% alertname = "success" if name == "notice" %>
      <% alertname = "danger" if name == "error" %>
      <%= content_tag :div, msg, class: "alert alert-#{alertname}" %>
    <% end %>
    <% if current_user %>
      <%= render partial: "shared/navbar" %>
    <% end %>
    <%= yield %>
  </div>
</body>
</html>
